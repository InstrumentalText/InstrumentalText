using UnityEngine;

// Parse json string generated from GPT api, execute action to targeted game object
public class Executor : MonoBehaviour
{
    void Execute(string jsonStr, GameObject target)
    {
        
    }
}
